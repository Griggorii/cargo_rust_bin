# cargo_rust_bin
dewlopment , my bin + example env

$ mkdir ~/.cargo && mkdir ~/.rustup && cd ~/.rustup && cat << EOF > settings.toml
telemetry = false
version = "12"

[overrides]
EOF && EOF

copy all binare folder cargo run folder all bin commnd:

$ cp -r * ~/.cargo

run locate check nemo , nautilus alternative explorer copy enter ~/.cargo


